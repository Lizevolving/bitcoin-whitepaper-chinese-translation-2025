# SEO / GSC / GA4 / GEO audit — bitcoin-whitepaper (2026-08-07)

- Live: https://bitcoin-whitepaper.lizliz.xyz/
- Repo: `/home/ubuntu/projects/bitcoin-whitepaper-chinese-translation-2025`
- Stack: Static HTML + client-rendered `zh.md` (marked/MathJax); Cloudflare Pages
- Evidence: source audit + live `curl` (2026-08-07). Fixes in source only. **No commit / no push.**
- Context: no project `AGENTS.md`. Did not touch `docs/AUDIT.md` (none).

## Live evidence (pre-fix)

| Probe | Result |
|---|---|
| `GET /` | HTTP 200; no `x-robots-tag`; meta `index, follow, max-image-preview:large` |
| `GET /robots.txt` | HTTP 200 `text/plain`; `Allow: /` + Sitemap (CF may prepend Content-Signals) |
| `GET /sitemap.xml` | HTTP 200 `application/xml`; `/` + `/zh.md`; lastmod was 2026-05-24 |
| `GET /zh.md` | HTTP 200 (source markdown) |
| `GET /progress/` | HTTP 200; draft EN-CN page; **no** robots/canonical/GA4/meta (indexable leak) |
| `GET /assets/og-image.png` | HTTP 200; **1200×630** PNG |
| GA4 in live HTML | Present `G-TXVLTJJ878` (head, async) |
| GSC meta / DNS TXT | Absent (not invented) |
| Static shell h1 | Absent pre-fix (`brand-title` was `<p>`; paper h1 only after JS renders `zh.md`) |

## Checklist

| Item | Status | Action taken | file:line |
|---|---|---|---|
| **GSC** sitemap.xml exists, valid, real URLs, sane lastmod | **fixed** | Kept `/` + `/zh.md`; bumped `lastmod` → 2026-08-07 | `sitemap.xml:5,11` |
| **GSC** robots.txt allows pages, references sitemap | **fixed** | Kept Allow + Sitemap; added `Disallow: /progress/` | `robots.txt:1-5` |
| **GSC** verification token | **residual** | None in source meta or DNS TXT; did not invent | — |
| **GSC** single canonical, no http/https/www duplicates | **ok** | Canonical https apex; no www host observed | `index.html:15` |
| **GSC** no noindex leaking onto indexable pages | **fixed** | Home stays `index, follow`; `/progress/` → `noindex, nofollow` + robots Disallow | `index.html:10`, `progress/index.html:8`, `robots.txt:3` |
| **GA4** gtag present with `G-TXVLTJJ878` | **ok** | Left intact | `index.html:553-558` |
| **GA4** ID intact (house rule) | **ok** | Unchanged | `index.html:553,558` |
| **GA4** head async placement | **ok** | Already in `<head>` async | `index.html:553` |
| **GEO** entity signals in visible copy | **fixed** | Brand subtitle: Satoshi 2008 + Liz 译本 + 章节/图示/公式 | `index.html:574` |
| **GEO** JSON-LD WebSite / Article / Person / Breadcrumb | **fixed** | Article `author` → Satoshi; `translator`/`publisher` → Liz; added Satoshi Person; synced descriptions + `dateModified` | `index.html:548-549` |
| **GEO** specific factual copy | **fixed** | Meta/schema cite Satoshi 2008, structure/figures/formulas/refs retained | `index.html:7,23,37,549` |
| **GEO** FAQ / How-it-works / About | **residual** | Reading shell + paper abstract cover substance; no invented FAQPage | — |
| **Base** `<title>` unique ~60 | **fixed** | `比特币白皮书中文翻译 2025 — Liz` (21) | `index.html:43` |
| **Base** meta description 120–160 pain-first | **fixed** | 124 chars; OG/Twitter/manifest synced | `index.html:7,23,37`, `site.webmanifest:3` |
| **Base** OG + Twitter + 1200×630 image | **ok / synced** | Full card set; titles/descs/author updated; image already 1200×630 | `index.html:19-41`, `assets/og-image.png` |
| **Base** hreflang / lang | **ok** | `lang="zh-CN"`; zh-CN + x-default (ZH-primary translation site; no separate EN URL) | `index.html:2,16-17` |
| **Base** one h1, logical h2/h3 | **fixed** | Static shell `<h1>` matching paper title (replaced when `zh.md` renders → sole content h1 + `##` sections) | `index.html:602`, `zh.md` |
| **Base** favicon + apple-touch + theme-color | **ok** | Already present | `index.html:11,44-46` |
| **Base** JSON-LD syntactically valid | **ok / improved** | Absolute URLs; distinct `@id`s; truthful author/translator split | `index.html:548-549` |

## Changes made (working tree, uncommitted)

- `index.html` — longer pain-first meta/OG/Twitter; title format; static h1; entity subtitle; JSON-LD author/translator fix; `article:modified_time`
- `robots.txt` — Disallow `/progress/`
- `sitemap.xml` — lastmod 2026-08-07
- `progress/index.html` — `noindex, nofollow`
- `site.webmanifest` — description sync
- `docs/audit-2026-08-07-seo.md` — this file

## Verification

- No `package.json` / build pipeline — **static Pages site**. Skipped build/typecheck (touched static HTML/meta/robots/sitemap only; no bundler config).
- Validated locally: JSON-LD `JSON.parse` OK; desc length 124; GA4 ID intact; static h1 present; progress noindex present.

## Residual

| Priority | Item | Notes |
|---|---|---|
| **P0** | — | None blocking crawl/index of `/` after ship |
| **P1** | GSC property verification | Add meta or DNS TXT when Liz owns the token; then submit sitemap |
| **P1** | Deploy | Source fixed; live still has short meta / indexable `/progress/` until Hermes commit+push |
| **P1** | CF managed AI crawler blocks | Live `robots.txt` may prepend GPTBot/ClaudeBot/etc. Disallows — hurts GEO retrieval; change only in Cloudflare dashboard if AI citation crawl is desired |
| **P2** | Dedicated FAQ / About block | Optional GEO boost; only with visible Q&A (no orphan FAQPage) |
| **P2** | `/progress/` draft cleanup | noindex+Disallow is enough; optional remove from public deploy later |
| **P2** | EN alternate URL | Site is ZH-primary translation; progress EN-CN is draft — no truthful `hreflang="en"` target yet |

## Confidence

- **High:** crawl baseline (robots/sitemap/canonical/GA4/OG), meta length, schema author/translator truth, progress noindex.
- **Medium:** GSC ownership unknown without token/dashboard; CF robots overlay not fully controllable from repo.
- **Owner:** Liz / Hermes for ship + GSC token + optional CF AI-bot policy.
- **Next checkpoint:** after Hermes commit+push — curl `/` meta desc, `/robots.txt` Disallow progress, `/progress/` noindex on prod; submit sitemap in GSC.
